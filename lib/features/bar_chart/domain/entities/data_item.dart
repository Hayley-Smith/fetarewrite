class DataItem {
  final int xaxis;
  final int yaxis;

  DataItem({
    this.xaxis,
    this.yaxis,
  });

  // will probably need to make this into an abstract in order to
  // have multiple versions of DataItems.
}
